# HTTP requests library for fetching web content
requests>=2.32.0

# HTML parsing and processing
beautifulsoup4>=4.12.0
markdownify>=0.11.0

# Document processing
docling>=0.1.0  # Document converter library

# Core dependencies that might be required by other packages
python-dateutil>=2.8.2
urllib3>=2.0.0
certifi>=2023.7.22
charset-normalizer>=3.2.0
idna>=3.4

# Optional but recommended for better HTML parsing
lxml>=4.9.0

# Development dependencies
pytest>=7.4.0  # For running tests
black>=23.7.0  # For code formatting
flake8>=6.1.0  # For code linting
mypy>=1.5.0   # For type checking

# Environment and packaging
python-dotenv>=1.0.0  # For environment variables if needed
setuptools>=68.0.0
wheel>=0.41.0

# Additional dependencies that might be needed based on usage
Pillow>=10.0.0  # For image processing if needed
PyYAML>=6.0.1  # For YAML processing if needed
fastapi